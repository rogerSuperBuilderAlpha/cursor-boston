# Required in production. GitHub → Settings → Webhooks → Secret.
GITHUB_WEBHOOK_SECRET=replace-with-a-long-random-string

# Optional: restrict accepted repos (owner/repo). Leave empty to accept any repo.
# GITHUB_ALLOWED_REPO=your-org/your-repo
