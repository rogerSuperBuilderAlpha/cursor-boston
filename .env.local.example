# =============================================================================
# Environment Variables Configuration
# =============================================================================
# Copy this file to .env.local and replace all placeholder values with your
# actual configuration. Never commit .env.local to version control.
#
# PLACEHOLDERS TO REPLACE:
#   - your-org          → Your GitHub organization or username
#   - your-project      → Your Firebase project ID
#   - cursorboston.com → Your domain name
#   - your-*-id         → Your actual IDs from respective services
# =============================================================================

# -----------------------------------------------------------------------------
# Firebase Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# Get these values from Firebase Console:
# https://console.firebase.google.com → Project Settings → General → Your apps
# -----------------------------------------------------------------------------
NEXT_PUBLIC_FIREBASE_API_KEY=your-api-key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
NEXT_PUBLIC_FIREBASE_APP_ID=your-app-id
NEXT_PUBLIC_FIREBASE_DATABASE_URL=https://your-project.firebaseio.com

# Optional: Firebase Analytics Measurement ID
# NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# -----------------------------------------------------------------------------
# Discord OAuth (OPTIONAL - for Discord connection feature)
# -----------------------------------------------------------------------------
# 1. Create a Discord application at: https://discord.com/developers/applications
# 2. Go to OAuth2 → General
# 3. Add redirect URI: https://YOUR-DOMAIN.com/api/discord/callback
# 4. Copy Client ID and create a Client Secret
# 5. Get your Discord Server ID (right-click server → Copy Server ID)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_DISCORD_CLIENT_ID=your-discord-client-id
DISCORD_CLIENT_SECRET=your-discord-client-secret
NEXT_PUBLIC_DISCORD_REDIRECT_URI=https://cursorboston.com/api/discord/callback
CURSOR_BOSTON_DISCORD_SERVER_ID=XXXXXXXX

# -----------------------------------------------------------------------------
# GitHub OAuth (OPTIONAL - for GitHub connection feature)
# -----------------------------------------------------------------------------
# 1. Go to: https://github.com/settings/developers
# 2. Click "New OAuth App"
# 3. Set Authorization callback URL: https://YOUR-DOMAIN.com/api/github/callback
# 4. Copy Client ID and create a Client Secret
# 5. For webhooks: Go to repository Settings → Webhooks → Add webhook
# -----------------------------------------------------------------------------
NEXT_PUBLIC_GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret
NEXT_PUBLIC_GITHUB_REDIRECT_URI=https://cursorboston.com/api/github/callback

# GitHub Webhook (OPTIONAL - for PR tracking)
# Generate a secret and configure it in your GitHub repository webhook settings
GITHUB_WEBHOOK_SECRET=your-github-webhook-secret
GITHUB_REPO_OWNER=your-org
GITHUB_REPO_NAME=cursor-boston

# -----------------------------------------------------------------------------
# Firebase Admin (OPTIONAL - required for GitHub webhook processing)
# -----------------------------------------------------------------------------
# Service account JSON from Firebase Console → Project settings → Service accounts
# Store the entire JSON as a single line string
FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"your-project-id"}

# -----------------------------------------------------------------------------
# Admin Email (OPTIONAL - for submission notifications)
# -----------------------------------------------------------------------------
# Email address that will receive notifications for talk and event submissions
# Defaults to hello@cursorboston.com if not set
# ADMIN_EMAIL=admin@yourdomain.com

# =============================================================================
# IMPORTANT NOTES:
# =============================================================================
# 1. Replace "cursorboston.com" with your actual domain in redirect URIs
# 2. Replace "your-org" with your GitHub organization/username
# 3. Replace "your-project" with your Firebase project ID
# 4. All NEXT_PUBLIC_* variables are exposed to the browser
# 5. Variables without NEXT_PUBLIC_ are server-side only
# 6. Never commit secrets or API keys to version control
# =============================================================================
